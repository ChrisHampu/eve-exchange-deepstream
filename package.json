{
  "name": "eve-exchange-deepstream",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "babel -d build server.js && babel -d build deepstream_auth.js && node build/server.js",
    "build": "babel -d build server.js && babel -d build deepstream_auth.js",
    "run": "node build/server.js > deepstream_log.txt 2>&1 &"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.16.0",
    "babel-preset-es2015": "^6.14.0"
  },
  "dependencies": {
    "deepstream.io": "^2.2.2",
    "deepstream.io-client-js": "^2.1.5",
    "node-fetch": "^1.6.3"
  }
}
